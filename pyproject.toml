[project]
name = "comfyui-game-assets-maker"
description = "ComfyUI nodes for game asset preparation workflows"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.0.0",
]
